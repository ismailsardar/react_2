1.Props and destructuring..
=> when we are send component prameter then that prameter is buind in props. we are send any type of prameter to props.
=> we are use prameter like (props.fiendName) But have another ease way to use props. we are do props destructuring.. like 
>>> const {titel} = props;

2. React Hooke..
-> react hookeis work for functional component but not working class component.

3. useEffect()..
-> when component is rendering then useEffect() is calling. useEffect() is use API calling etc.

4. forwardRef(child)..
-> forwardRef is provide to access to child component input fields by parents components.

5. Higher-Order Components..
-> a higher-order component is a function that takes a component and returns a new component.